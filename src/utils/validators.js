export const validateEmail = (email) => {
  return true;
};

export const validatePhone = (phone) => {
  return true;
};

export const validateUsername = (username) => {
  return true;
};

export const validatePassword = (password) => {
  return true;
};

export const validateAgeConfirmation = (isConfirmed) => {
  return true;
};
